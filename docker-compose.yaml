version: "3.8"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8080:8080
    depends_on:
      - backend

  backend:
    build:
      context: backend
    ports:
      - 8000:8000
